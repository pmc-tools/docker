[tool.pyright]
include = ["umbtest", "tests", "main.py"]
stubPath = "stubs"
pythonVersion = "3.12"
typeCheckingMode = "strict"

[tool.ruff]
[tool.ruff.lint.per-file-ignores]
"stubs/**/*.pyi" = ["I001"]